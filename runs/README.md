This folder should contain the previously saved models. The structure should be as follows:
-- run name
    -- checkpoint name
        -- pytorch_model.bin
        -- config.json
        -- training_args.bin
        -- etc...